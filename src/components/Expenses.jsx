import React from 'react';
// import PropTypes from 'prop-types';
// import { connect } from 'react-redux';

class Expenses extends React.Component {
  render() {
    return <div>Tabela de gastos</div>;
  }
}

export default Expenses;
