import React from 'react';
import { Header, ExpenseForm, WalletPanel } from '../components';

const Wallet = () => (
  <div>
    <Header />
    <ExpenseForm />
    <WalletPanel />
  </div>
);

export default Wallet;
