import { login } from './user';
import { addExpense, requestFail, requestStarted, requestCurrencies } from './wallet';

export default {
  login,
  addExpense,
  requestFail,
  requestStarted,
  requestCurrencies,
};
