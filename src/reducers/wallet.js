const INICIAL_STATE = {
  currencies: [],
  expenses: [],
};

const userWallet = (state = INICIAL_STATE, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default userWallet;
