const INICIAL_STATE = {
  currencies: ['BRL'],
  expenses: [0],
};

const userWallet = (state = INICIAL_STATE, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default userWallet;
